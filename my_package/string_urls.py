def upper_case(text: str) -> str:
    """A function that returns the text in upper case."""
    return text.upper()


def stripped_text(text: str) -> str:
    """A function that removes leading and trailing whitespace from the text."""
    return text.strip()